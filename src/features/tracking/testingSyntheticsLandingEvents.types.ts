/** Product panel shown on the Testing & synthetics landing page. */
export type LandingProduct = 'agentic' | 'performance' | 'synthetics';

/** Synthetic monitoring action tile on the landing page. */
export type SyntheticsTile = 'make-check' | 'manage-probes' | 'terraform';
