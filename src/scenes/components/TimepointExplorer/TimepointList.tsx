import React from 'react';
import { TimeRange } from '@grafana/data';

interface TimepointListProps {
  timeRange: TimeRange;
}

export const TimepointList = ({ timeRange }: TimepointListProps) => {
  return (
    <div>
      <div>TimepointList</div>
    </div>
  );
};
